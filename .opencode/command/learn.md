---
description: Extract evidence-backed learning from this session into owner-reviewed proposals
agent: learning-observer
---

Use the `self-improvement` skill to analyze this session and the current repository state.

Record only concise, sanitized, reproducible evidence. Create at most three deduplicated proposals under `.opencode/learning/proposals/` and append a journal entry. Do not modify `AGENTS.md`, skills, agents, commands, configuration, product code, dependencies, generated files, or any other active project behavior. Do not approve, implement, commit, or push a proposal.

Focus on non-obvious discoveries such as hidden relationships, surprising execution paths, misleading errors, repository invariants, files that must change together, and undocumented build or verification constraints. Ignore obvious facts, standard framework behavior, temporary workarounds, and guidance already present in the repository.

If the evidence is weak or an owner gate is required, record the uncertainty and stop.

Additional scope or evidence: $ARGUMENTS
