---
description: Implement one explicitly owner-approved learning proposal and request independent review
agent: improvement-implementer
---

Implement proposal `$ARGUMENTS`. Load the `self-improvement` skill and verify the recorded owner approval, evidence, scope, validation, rollback, dedicated branch, and clean worktree before editing. Implement only the approved hypothesis, run relevant checks from the correct package directories, invoke `improvement-reviewer`, and leave the proposal awaiting the machine owner's final decision. Do not commit, push, merge, publish, or mark the proposal adopted.
